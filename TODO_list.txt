TODO :
	Server :
		- Handle all exceptions.
		- Code lights module in a better way, to be more flexible with other configurations.
		- Update documentation, pass it into Markdown.
		- Add a way to add users on a "database" (use a simple file).
		- Add a way to use authentification with the Android application.
		- Make a signature in order to be sure that no modification has be done on our code.
		- Add a new client for desktop users.
		- Add a dashboard.
		- Add a way to create scenarios.
		- Find modules to add.
		- Add the possibility to pass by an official proxy server with authentification to use the system outside the LAN.

	AndroidApp :
		- Add an alarm clock connected to the system (for scenarios).
		- Manage the scrollbar list of the left side (deal with all buttons).
			* Settings.
			* Add widget.
			* Personalise a widget.
			* Logout.
			* Account.
		- Synchronize information with server's file

Done this week :
	Server :
		- Fix all problems with lights.
		- Send logs to the client.
		- Send errors to the client.
	
	Android :
		- Add login Activity.
		- Generate login file (ip@ , port , email , encrypted password).
		- Make the scrollbar list of the left side interactive (deal with all buttons).
			* Settings.
			* Add widget.
			* Logout.
			* Account.
